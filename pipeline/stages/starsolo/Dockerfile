# spot pipeline stage: starsolo  (built + pinned by @sha256 in the dataset manifest)
# TODO: pin base image by digest; add entrypoint. Placeholder for the lane foundation.
FROM python:3.12-slim
LABEL org.spot.stage="starsolo"
