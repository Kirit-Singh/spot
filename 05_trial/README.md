# 05_trial — trial-design synopsis (v1 placeholder)

**v1: a light placeholder.** Carries the locked drug + its scores; the trial-synopsis
logic (adjuvant vs neoadjuvant, population, endpoints, PD biomarker) is deferred, to be
refined in a later pass. Decision-support only — never an actual trial design.

- `inputs/`  — the locked score card from 04
- `analysis/` — (later) CS workbook: precedent search + synthesis
- `outputs/` — (later) trial-rationale synopsis
