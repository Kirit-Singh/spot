# 05_trial — trial-design synopsis

Synthesize a **decision-support** trial synopsis. **Locks:** the trial design.

Runs as a Claude Science **specialist** (project `spot · 05 trial`): clinical-trial
decision-support — setting (adjuvant vs neoadjuvant), population (newly-dx vs
recurrent; MGMT), dosing (from 04), endpoints with **Treg-program reduction as the
PD biomarker**, + precedents (ClinicalTrials.gov).

> Decision-support only — a synopsis for clinical/regulatory/safety experts, NOT an
> actual trial design.

- `inputs/`  — the locked score card from 04
- `analysis/` — CS workbook: precedent search + synthesis
- `outputs/` — trial-rationale synopsis (cited)
