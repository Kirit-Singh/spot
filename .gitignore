# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Node
node_modules/
dist/
.vite/
.wrangler/

# Data / scratch / outputs — NEVER commit data
.scratch/
/data/
outputs/
*.h5ad
*.h5mu

# Secrets / local (infra.env is tracked and non-secret; .env is NOT)
.env
.dev.vars*
!.dev.vars.example
*.local

# OS
.DS_Store

# pipeline runtime data (re-fetchable via manifest)
pipeline/datasets/

# e2e run outputs
pipeline/tests/e2e/work/
01_programs/analysis/_t8_staging/
# Stage-1 -> Stage-2 bridge: generated/large release bundle (projections + hardlinked parquet)
01_programs/analysis/stage2_bridge/_release_staging/
