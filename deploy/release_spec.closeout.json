{
  "_comment": "CLOSEOUT release spec. Slots are exact where the shape is known; every 'src' and every hash is null until the orchestrator supplies the real admitted bytes. Nothing here is invented. As shipped this REFUSES (every lane is PENDING). Flip a lane to ADMIT only when its production receipt admits.",
  "_slot_provenance": {
    "stage2": "Symbol-bound display projection from W3 commit bc3b10b (origin/agent/stage2-runmanifest-round4). Artifact filename, schema and receipt shape are taken from that commit: display_projection.PROJECTION_FILE='stage2_display_projection.json', SCHEMA='spot.stage02_display_projection.v2', verifier_id='spot.stage02.display_projection.independent_verifier.v1', receipt verdict is 'admit'|'reject' and its subject.projection_raw_sha256 names the exact bytes judged. 'bound_by_receipt' makes the assembler refuse an artifact the receipt did not judge.",
    "pathway_go_bp_rest": "GO-BP-ONLY CRITICAL PATH. NOT YET PRODUCED: no emitted pathway artifact or receipt is committed on any branch, so no path or hash is filled here. Producer (verified from origin/agent/stage2-runmanifest-round4): `python -m direct.release_inventory --lane pathway` is the ONE pathway inventory builder -> emits 'pathway_arm_release.json' (schema spot.stage02_pathway_arm_release.v1). Its external admission report must declare the PATHWAY lane's own schema 'spot.stage02_pathway_arm_external_admission.v1' (a temporal report may NOT admit the pathway lane). The Direct/condition admission receipt is 'w10_admission_{condition}.json' -> for Rest: 'w10_admission_Rest.json' (schema spot.stage02_direct_arm_bundle_verification.v1). genesets.py requires GO-BP to name a DATED release_id and pins the GMT's raw sha256 - the file on disk must hash to the pin; 'pathway_collection: go_bp' makes the assembler enforce all of that.",
    "reactome": "PARKED. Not on the critical path, not required, and never advertised in the deployable UI bundle. The assembler refuses pathway_collection='reactome', refuses a pathway artifact that names reactome, and refuses a dist file that advertises it. Its license (CC0-1.0) stays recorded in the inventory as parked, for the record only.",
    "stage3": "PENDING — 03_druglink is specified but not implemented; artifact shape unknown, so no dst is guessed. Its 'consumes' block is the gate: Stage-3 may only rest on an INDEPENDENTLY ADMITTED Stage-2 artifact, named by its exact sha256.",
    "stage4": "PENDING — 04_PKPD is specified but not implemented; artifact shape unknown, so no dst is guessed. Same 'consumes' gate against Stage-3."
  },
  "release_id": null,
  "lanes": {
    "stage1": {
      "status": "PENDING",
      "route": "/01_page.html",
      "receipt": {"src": null, "dst": "lanes/stage1/receipt.json", "expected_sha256": null},
      "artifacts": [
        {"src": null, "dst": "stage01_program_registry_v3.json", "expected_sha256": null},
        {"src": null, "dst": "stage01_stage2_registry_view.json", "expected_sha256": null}
      ]
    },
    "stage2": {
      "status": "PENDING",
      "route": "/02_page.html",
      "receipt": {"src": null, "dst": "lanes/stage2/receipt.json", "expected_sha256": null},
      "artifacts": [
        {"src": null, "dst": "stage2_display_projection.json", "expected_sha256": null, "bound_by_receipt": true},
        {"src": null, "dst": "pathway_arm_release.json", "expected_sha256": null, "pathway_collection": "go_bp",
         "_condition": "Rest",
         "_requires": "a DATED GO-BP release_id + a 64-hex GMT byte pin; the GMT on disk must hash to the pin. Reactome is parked and refused."}
      ]
    },
    "stage3": {
      "status": "PENDING",
      "route": "/03_page.html",
      "receipt": {"src": null, "dst": "lanes/stage3/receipt.json", "expected_sha256": null},
      "consumes": [
        {"lane": "stage2", "artifact_sha256": null,
         "_note": "the exact sha256 of the independently admitted Stage-2 artifact Stage-3 reads. Null refuses: a consumer may not rest on bytes nobody admitted."}
      ],
      "artifacts": [
        {"src": null, "dst": null, "expected_sha256": null}
      ]
    },
    "stage4": {
      "status": "PENDING",
      "route": "/04_page.html",
      "receipt": {"src": null, "dst": "lanes/stage4/receipt.json", "expected_sha256": null},
      "consumes": [
        {"lane": "stage3", "artifact_sha256": null,
         "_note": "the exact sha256 of the independently admitted Stage-3 artifact Stage-4 reads."}
      ],
      "artifacts": [
        {"src": null, "dst": null, "expected_sha256": null}
      ]
    }
  },
  "dist": {
    "_comment": "Prebuilt UI dist for the Cloudflare lane, e.g. produced by deploy/build_dist.sh <dist-dir>. Every file is scanned + hashed and staged under dist/. Leave src null until the UI build is the reviewed source build.",
    "src": null
  },
  "hf": {
    "_comment": "HF packaging checks. The immutable source revision must be a real 40-hex revision; stage1_release_hf_revision must stay null until a real upload returns one.",
    "card": "01_programs/hf_release/STAGE1_V3_DATASET_CARD.template.md",
    "manifest": "01_programs/hf_release/stage1_release_hf_manifest.template.json"
  }
}
